<div class="container jumbotron">
  <div class="page-header">
    <h1><span class="icon ion-power"> </span> Hello, world!</h1>
  </div>
  <p class="lead">I am a full time hacker who has a passion for finding creative,
    efficient, and elegant solutions to practical problems.</p>
  <p>This is a playground for ideas that can double as a showcase of my work. I
    hope you find something interesting, and make sure you check out our baby,
    <a href="https://sparehanger.com" target="_blank">sparehanger</a> &#8212; a new way
    to find, organize, and share your style.</p>
  <p>Thanks for visiting!</p>
  <p>-Selby</p>
</div>
<div class="container">
  <div class="page-header">
    <h2><span class="icon ion-social-hackernews"> </span> <a href="https://news.ycombinator.com/">Hacker News</a></h2>
  </div>
  {{#each item in model}}
      {{#if item.title}}
              {{#if item.isUrl}}
                <h4> <a {{bind-attr href=item.url}}>{{item.title}}</a><small> ({{item.domain}}) </small></h4>
              {{else}}
                <h4> <a {{bind-attr href=item.commentsUrl}}>{{item.title}}</a></h4>
              {{/if}}
              <p class="text-right">
                {{#unless item.isJob}}<span>{{item.score}} points</span> by <a {{bind-attr href=item.authorUrl}}>{{item.by}}</a> {{/unless}}{{item.publishedDate}}{{#unless item.isJob}} | <a {{bind-attr href=item.commentsUrl}}>comments</a>{{/unless}}
              </p>
              <hr>
      {{/if}}
  {{/each}}
</div>
